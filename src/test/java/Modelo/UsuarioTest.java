package Modelo;

import org.junit.Test;

public class UsuarioTest {

    // hacer test de crear usuario con datos válidos

    // hacer test de que el ID se genera automáticamente

    // hacer test de que el username se guarda correctamente

    // hacer test de que el createdAt se genera automáticamente

    // hacer test de los getters (getId, getUsername, etc)

    // hacer test de los setters (setPasswordHash, setSalt)

    // hacer test de que el constructor sin parámetros funciona (para Gson)

}

package Controlador;

import org.junit.Test;

public class InstantAdapterTest {

    // hacer test de que el formato JSON es correcto
    // hacer test de que el formato JSON es incorrecto

    // hacer test de que serializar y deserializar devuelve el mismo valor
    // hacer test con diferentes fechas (pasado, presente, futuro)
}

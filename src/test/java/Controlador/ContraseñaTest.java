package Controlador;

import org.junit.Test;

public class ContraseñaTest {

    // hacer test de generar salt aleatorio
    // hacer test de hashear una contraseña
    // hacer test de que el hash no es vacío

    // hacer test de verificar contraseña correcta
    // hacer test de verificar contraseña incorrecta

    // hacer test de que el mismo password con mismo salt da el mismo hash
    // hacer test de que el mismo password con diferente salt da diferente hash

    // hacer test de limpiar array de contraseña (zero)
    // hacer test de que después de zero el array está lleno de '\0'

}

package Controlador;

import org.junit.Test;

public class UserStoreTest {

    // hacer test de crear UserStore con archivo que no existe
    // hacer test de que se crea el directorio data si no existe

    // hacer test de guardar un usuario nuevo
    // hacer test de buscar usuario por username
    // hacer test de buscar usuario que no existe (debe devolver Optional.empty())

    // hacer test de que el username es case-insensitive (ej: "Miguel" = "miguel")

    // hacer test de que exists devuelve true si el usuario existe
    // hacer test de que exists devuelve false si el usuario no existe

    // hacer test de actualizar un usuario existente
    // hacer test de listar todos los usuarios
    // hacer test de que no se puede guardar usuario duplicado

    // hacer test de que el archivo JSON se crea correctamente
    // hacer test de que se puede cargar usuarios desde el archivo JSON

}
